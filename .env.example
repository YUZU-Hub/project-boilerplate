# Project Environment Variables
# Copy this file to .env and fill in your values
#
# Usage: cp .env.example .env

# =============================================================================
# Node.js
# =============================================================================
NODE_ENV=development

# =============================================================================
# Server Ports
# =============================================================================
# Homepage server port
HOMEPAGE_PORT=3000

# Webapp server port (leave empty to mount on homepage)
WEBAPP_PORT=3001

# Path prefix for webapp when mounted on homepage (when WEBAPP_PORT is empty)
WEBAPP_PATH=/

# Admin server port (leave empty to mount on homepage)
ADMIN_PORT=3002

# Path prefix for admin when mounted on homepage (when ADMIN_PORT is empty)
ADMIN_PATH=/admin

# =============================================================================
# PocketBase
# =============================================================================
# API URL for client-side JavaScript
API_URL=http://localhost:8090

# =============================================================================
# MCP Server Credentials (add to ~/.zshrc, not here!)
# =============================================================================
# These environment variables are used by Claude Code's MCP integrations.
# Add them to your shell profile (~/.zshrc or ~/.bashrc), NOT this file.
# After adding, run: source ~/.zshrc
#
# REQUIRED - PocketBase MCP (database operations)
# Create these when you first access http://localhost:8090/_/
# export POCKETBASE_ADMIN_EMAIL="admin@example.com"
# export POCKETBASE_ADMIN_PASSWORD="your-password"
#
# REQUIRED - GitHub MCP (repo management)
# Create at: https://github.com/settings/tokens (scopes: repo, read:org)
# export GITHUB_PERSONAL_ACCESS_TOKEN="ghp_xxx"
