# Shared Credentials - Add these to your shell profile (~/.zshrc or ~/.bashrc)
# These are used across all projects and should NOT be committed to git.
#
# Copy the export statements below to your shell profile:

# =============================================================================
# GitHub
# =============================================================================
export GITHUB_PERSONAL_ACCESS_TOKEN=""          # https://github.com/settings/tokens

# =============================================================================
# Coolify
# =============================================================================
export COOLIFY_URL="https://coolify.your-server.com"
export COOLIFY_TOKEN=""                         # Coolify → Settings → API Tokens

# =============================================================================
# Shared Hosting (SSH/SFTP)
# =============================================================================
export SHARED_HOST_SSH_HOST=""                  # e.g., www453.your-server.de
export SHARED_HOST_SSH_PORT="22"                # or 222 for some hosts
export SHARED_HOST_SSH_USER=""
export SHARED_HOST_SSH_KEY="~/.ssh/id_rsa"      # Path to SSH private key

# =============================================================================
# PocketBase Admin (for local development)
# =============================================================================
export POCKETBASE_ADMIN_EMAIL=""
export POCKETBASE_ADMIN_PASSWORD=""
