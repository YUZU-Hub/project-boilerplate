# Shared Credentials - Add these to your shell profile (~/.zshrc or ~/.bashrc)
# These are used across all projects and should NOT be committed to git.
#
# Copy the export statements below to your shell profile.
# After adding, run: source ~/.zshrc

# =============================================================================
# REQUIRED - PocketBase Admin (for PocketBase MCP server)
# =============================================================================
# Used by Claude Code to interact with PocketBase via MCP.
# Create these credentials when you first access PocketBase Admin UI.
export POCKETBASE_ADMIN_EMAIL=""
export POCKETBASE_ADMIN_PASSWORD=""

# =============================================================================
# REQUIRED - GitHub (for GitHub MCP server)
# =============================================================================
# Used by Claude Code for repository operations via MCP.
# Create token at: https://github.com/settings/tokens
# Required scopes: repo, read:org
export GITHUB_PERSONAL_ACCESS_TOKEN=""

# =============================================================================
# OPTIONAL - Coolify (only if using Coolify for deployment)
# =============================================================================
# Used by Claude Code to manage Coolify deployments via MCP.
# Get token from: Coolify Dashboard → Settings → API Tokens
export COOLIFY_URL="https://coolify.your-server.com"
export COOLIFY_TOKEN=""
