name: Deploy API

on:
  push:
    branches:
      - main
    paths:
      - 'api/**'
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual deployment'
        required: false
        default: 'Manual trigger'

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Deployment info
        run: |
          echo "API deployment triggered"
          echo "Trigger: ${{ github.event_name }}"
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "Reason: ${{ github.event.inputs.reason }}"
          fi
          echo ""
          echo "Coolify will automatically deploy from this push."
          echo "Check your Coolify dashboard for deployment status."

  # Uncomment below if you want to run tests before Coolify deploys
  # test:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Build Docker image
  #       run: |
  #         cd api
  #         docker build -t pocketbase-test .
  #
  #     - name: Run health check
  #       run: |
  #         docker run -d --name pb-test -p 8090:8090 pocketbase-test
  #         sleep 5
  #         curl -f http://localhost:8090/api/health || exit 1
  #         docker stop pb-test
